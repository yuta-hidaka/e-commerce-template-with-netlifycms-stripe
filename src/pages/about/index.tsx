
import { NextPage } from 'next';
import ReactMarkdown from 'react-markdown';

const Cart: NextPage = () => {
  return <ReactMarkdown>
    {
        `
        `
    }
  </ReactMarkdown>;
};

export default Cart;
